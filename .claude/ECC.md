# Itens do ECC neste projeto

Copiados de https://github.com/affaan-m/ECC (commit e482e57), licença em `ECC-LICENSE`.
Só markdown: nenhum hook, script ou servidor MCP do ECC foi instalado.

## Skills (`.claude/skills/`)
- postgres-patterns, database-migrations: SQL, índices, RLS e migrações (Supabase/Postgres)
- security-review: revisão de segurança (auth, segredos, entrada, RLS)
- nextjs-turbopack, react-patterns, backend-patterns, api-design: app Next.js e rotas de API
- verification-loop, tdd-workflow: verificação antes de concluir e testes primeiro

## Agentes (`.claude/agents/`)
- database-reviewer, security-reviewer, typescript-reviewer, react-reviewer, code-reviewer, build-error-resolver

Para atualizar: copie de novo as mesmas pastas de `skills/` e `agents/` do repositório do ECC.
