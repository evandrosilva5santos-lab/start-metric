{
  "name": "@start-metric/db",
  "version": "0.0.0",
  "main": "index.ts",
  "types": "index.ts",
  "private": true,
  "scripts": {
    "build": "prisma generate",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "latest"
  },
  "devDependencies": {
    "prisma": "latest"
  }
}
